{
  "name": "notethat-electron",
  "version": "0.5.0",
  "description": "",
  "main": "src/main.js",
  "scripts": {
    "start": "tsc && electron .",
    "build": "tsc && electron-builder",
    "build-all": "tsc && electron-builder -mwl",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^29.0.1",
    "electron-builder": "^24.12.0",
    "typescript": "^5.3.3"
  }
}
